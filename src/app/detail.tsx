import {
    Dimensions,
    Image,
    ScrollView,
    StyleSheet,
    Text,
    View,
  } from "react-native";
import ArticleScroll from "../components/AriticleScroll";

const Detail = (): JSX.Element => {
  return (
    <ArticleScroll />
  )
}

export default Detail
